import React from 'react'
import styled from 'styled-components'

const CallGroup = styled 

const Cell = props => {
  <CellGroup>
    <CallImage image={props.image}></CallImage>
    <CallTitle>{props.title}</CallTitle>
  </CellGroup>
}

export default Cell